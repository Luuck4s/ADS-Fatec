/**
 * 4. Entrar via teclado com o valor da cotação do dólar e uma certa quantidade de dólares. Calcular e * exibir o valor correspondente em Reais (R$).
*/
void main() {
  double cotDolar = 5.25;
  double dolares = 69;
  
  double reais = dolares * cotDolar;
 
  print(reais);
}